@extends('admin.layouts.index')

@section('content')
<div class="content-wrapper">
  <section class="content-header">
    <div class="container-fluid">
      <div class="row mb-2">
        <div class="col-sm-12 d-flex align-items-center" style="gap: 12px;">
          <h1 class="m-0">{{ __('Рабочие часы по дням недели') }}</h1>
          <small class="text-muted">Используется в мониторинге для подсчёта звонков «в раб. время».</small>
        </div>
      </div>
    </div>
  </section>

  <section class="content">
    <div class="container-fluid">
      @if(session('success'))
        <div class="alert alert-success">{{ session('success') }}</div>
      @endif
      @if(session('error'))
        <div class="alert alert-danger">{{ session('error') }}</div>
      @endif

      <div class="card">
        <div class="card-body">
          <form method="POST" action="{{ route('admin.work-hours.update') }}">
            @csrf
            <table class="table table-bordered align-middle" style="max-width: 760px;">
              <thead>
                <tr>
                  <th style="width: 32%;">{{ __('День недели') }}</th>
                  <th class="text-center" style="width: 14%;">{{ __('Активен') }}</th>
                  <th class="text-center" style="width: 27%;">{{ __('Начало') }}</th>
                  <th class="text-center" style="width: 27%;">{{ __('Конец') }}</th>
                </tr>
              </thead>
              <tbody>
                @foreach($weekdayLabels as $weekday => $label)
                  @php
                    $row = $rows[$weekday] ?? ['start_hour' => 9, 'end_hour' => 18, 'is_active' => true];
                  @endphp
                  <tr>
                    <td><strong>{{ $label }}</strong></td>
                    <td class="text-center">
                      <input type="hidden" name="hours[{{ $weekday }}][is_active]" value="0">
                      <input type="checkbox" name="hours[{{ $weekday }}][is_active]" value="1"
                        {{ $row['is_active'] ? 'checked' : '' }}>
                    </td>
                    <td class="text-center">
                      <select name="hours[{{ $weekday }}][start_hour]" class="form-control" style="display:inline-block; width:auto;">
                        @for($h = 0; $h <= 23; $h++)
                          <option value="{{ $h }}" {{ (int)$row['start_hour'] === $h ? 'selected' : '' }}>
                            {{ sprintf('%02d:00', $h) }}
                          </option>
                        @endfor
                      </select>
                    </td>
                    <td class="text-center">
                      <select name="hours[{{ $weekday }}][end_hour]" class="form-control" style="display:inline-block; width:auto;">
                        @for($h = 1; $h <= 24; $h++)
                          <option value="{{ $h }}" {{ (int)$row['end_hour'] === $h ? 'selected' : '' }}>
                            {{ sprintf('%02d:00', $h) }}
                          </option>
                        @endfor
                      </select>
                    </td>
                  </tr>
                @endforeach
              </tbody>
            </table>

            <div class="mt-3">
              <button type="submit" class="btn btn-primary">{{ __('Сохранить') }}</button>
              <a href="{{ route('admin.monitoring') }}" class="btn btn-default">{{ __('К мониторингу') }}</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </section>
</div>
@endsection
